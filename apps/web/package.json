{
  "name": "@forca/web",
  "version": "1.0.0",
  "description": "Interface web estática do jogo da forca (engine roda no navegador)",
  "private": true,
  "scripts": {
    "build": "node esbuild.mjs build",
    "dev": "node esbuild.mjs dev",
    "preview": "node esbuild.mjs preview"
  },
  "dependencies": {
    "@forca/engine": "*"
  },
  "devDependencies": {
    "esbuild": "^0.21.0"
  },
  "license": "MIT"
}
